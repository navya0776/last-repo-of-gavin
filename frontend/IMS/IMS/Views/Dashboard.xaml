<Page x:Class="IMS.Views.Dashboard"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:local="clr-namespace:IMS.Views"
      mc:Ignorable="d" 
      Title="Dashboard" Background="#F5F6FA">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80"/>

            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- ============= LEFT ICON SIDEBAR ============= -->
        <StackPanel Grid.Column="0"                    
                    VerticalAlignment="Stretch"                    
                    >
            <StackPanel.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#076EF2" Offset="0.0"/>
                    <GradientStop Color="#81B5F7" Offset="1.0"/>
                </LinearGradientBrush>
            </StackPanel.Background>

            <Image Source="/Assets/logo.png"
                   Width="50" Height="50"
                   Margin="0,10"/>
            <Button Margin="0,30,0,0" Width="35" Background="Transparent" BorderBrush="Transparent" >
                <Image Source="/Assets/menu.png" />
            </Button>


        </StackPanel>

        <!-- ============= MAIN CONTENT ============= -->
        <Grid Grid.ColumnSpan="2" Margin="70,10,10,-10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="349*"/>
                <ColumnDefinition Width="371*"/>
            </Grid.ColumnDefinitions>
            <Grid.Background>
                <ImageBrush Opacity="1" Stretch="UniformToFill" ImageSource="/Assets/bg_gavinSpark.png"/>
            </Grid.Background>
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Grid.ColumnSpan="2" >
                <ContentControl Content="Dashboard"
                                FontSize="35"
                                FontWeight="ExtraBlack"
                                VerticalAlignment="Center"
                                Margin="20,15,0,0"/>
            </TextBlock>
            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Grid.ColumnSpan="2" Margin="0,0,0,0">
                <Button Content="Ledger &amp; OS" Width="300" Height="300"

            Style="{DynamicResource DashboardButtonStyle}" Click="Ledger_click" />

                <Button Content="Provisioning" Width="300" Height="300" VerticalAlignment="Center" Style="{DynamicResource DashboardButtonStyle}" Click="Provisioning_Click"/>
                <Button Content="Reciept" Width="300" Height="300" Style="{DynamicResource DashboardButtonStyle}" Click="Reciept_click"/>
            </StackPanel>

        </Grid>

    </Grid>
</Page>