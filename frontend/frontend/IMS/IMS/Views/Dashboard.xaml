<Page x:Class="IMS.Views.Dashboard"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:local="clr-namespace:IMS.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Dashboard" Background="#F5F6FA">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80"/>

            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- ============= LEFT ICON SIDEBAR ============= -->
        <StackPanel Grid.Column="0"                    
                    VerticalAlignment="Stretch"                    
                    >
            <StackPanel.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#076EF2" Offset="0.0"/>
                    <GradientStop Color="#81B5F7" Offset="1.0"/>
                </LinearGradientBrush>
            </StackPanel.Background>

            <Image Source="/Assets/logo.png"
                   Width="50" Height="50"
                   Margin="0,10"/>
            <Button Margin="0,30,0,0" Width="35" Background="Transparent" BorderBrush="Transparent" >
                <Image Source="/Assets/menu.png" />
            </Button>
            <Button Margin="0,20,0,0" Width="35" Background="Transparent" BorderBrush="Transparent" >
                <Image Source="/Assets/help.png" />
            </Button>
            <Button Margin="0,20,0,0" Width="35" Background="Transparent" BorderBrush="Transparent" >
                <Image Source="/Assets/alert.png" />
            </Button>



        </StackPanel>

        <!-- ============= MAIN CONTENT ============= -->
        <Grid Grid.Column="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="349*"/>
                <ColumnDefinition Width="371*"/>
            </Grid.ColumnDefinitions>
            <Grid.Background>
                <ImageBrush Opacity="1" Stretch="UniformToFill" ImageSource="/Assets/bg_gavinSpark.png"/>
            </Grid.Background>
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="*" />
                <RowDefinition Height="*"/>

            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Grid.ColumnSpan="2" >
                <ContentControl Content="Dashboard"
                                FontSize="35"
                                FontWeight="ExtraBlack"
                                VerticalAlignment="Center"
                                Margin="20,15,0,0"/>
            </TextBlock>
            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Left" Grid.ColumnSpan="2" Margin="100,0,0,0">
                <Button Content="Ledger &amp; OS"

            Style="{DynamicResource DashboardButtonStyle}" Click="Ledger_click" />

                <Button Content="Provisioning" Width="150" Height="150" Margin="10" Style="{DynamicResource DashboardButtonStyle}" Click="Provisioning_click"/>
                <Button Content="Reciept" Width="150" Height="150" Margin="10" Style="{DynamicResource DashboardButtonStyle}" Click="Reciept_click"/>
            </StackPanel>
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Left" Grid.ColumnSpan="2" Margin="190,0,0,0">
                <Button Content="Issue" Width="150" Height="150" Margin="10" Style="{DynamicResource DashboardButtonStyle}" Click="Issue_click"/>
                <Button Content="Alert" Width="150" Height="150" Margin="10" Style="{DynamicResource DashboardButtonStyle}" Click="Alert_click"/>
            </StackPanel>
        </Grid>

    </Grid>
</Page>
