<Window x:Class="IMS.Windows.RangeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Range"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        WindowStyle="ToolWindow">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Select Range" FontSize="18" FontWeight="Bold" Margin="0,0,0,12"/>

        <StackPanel Grid.Row="1" Orientation="Vertical" Margin="0,4,0,0">
            <StackPanel Orientation="Horizontal" Margin="0,6">
                <TextBlock Text="From:" Width="60" VerticalAlignment="Center"/>
                <TextBox x:Name="FromTextBox" Width="180" Height="26"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,6">
                <TextBlock Text="To:" Width="60" VerticalAlignment="Center"/>
                <TextBox x:Name="ToTextBox" Width="180" Height="26"/>
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Cancel" Width="80" Margin="0,0,8,0" Click="Cancel_Click"/>
            <Button Content="Add" Width="80" Click="Add_Click"/>
        </StackPanel>
    </Grid>
</Window>
